services:
  posts:
    image: posts
    hostname: posts
    build:
      context: .
      dockerfile: ./Dockerfile
    restart: unless-stopped
    environment:
      STORAGE_TYPE: "inmemory"
      GRAPHQL_PORT: "8080"
    ports:
      - "8080:8080"
